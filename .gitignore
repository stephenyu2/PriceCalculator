# Environment variables — never commit these
.env
.env.local
.env*.local

# macOS
.DS_Store

# Curriculum pipeline run artifacts (transient; blueprints are kept)
library/pipeline/staging/
library/pipeline/verdicts/
library/pipeline/feedback/
library/pipeline/flagged/
library/pipeline/reports/

# pipeline python venv (sympy/numpy for the verifier)
library/pipeline/.venv/

# Local Netlify folder
.netlify

# Claude Code local settings (machine-specific)
.claude/
